- Work with Claude to create the schema
- Build POSTgres database in replit using schema
- Insert data into database
- Update API layer to use database with storage interface

- Build agents to pull data (create agent loops)
- Use LLM as judge (one grabs data, another checks it)